class CryptoManager {
    init {
        // Crypto Setup, Create Cert, Create Public Key
    }
    public fun getPublicKey(): String {
        return "test_publi_key"
    }
    public fun encryption(data:String) {}
    public fun decryption(publicKey:String, data:String) {}
}